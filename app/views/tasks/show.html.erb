<h2> <%= @task.title %> </h2>
<p> <%= @task.details %> </p>
  <%= form_for(@task) do |f| %>
    <%= f.label :completed %>
    <%= f.check_box :completed %>
<% end %>
<hr>
<%= link_to('back to list', tasks_path) %>
